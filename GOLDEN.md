# GOLDEN.md — reproducibility anchors

Generated by src/pipeline.py on the Phase 0 build (seed 7, demo data).
Executor: re-run both commands below before trusting any new analysis.
If values drift beyond the 4th decimal, STOP and post a pm-update
(CLAUDE.md rule 3). `python -m pytest tests/ -q` must also pass (4 tests).

## Command 1: planted reflective (rank-1) structure
    python src/pipeline.py --demo reflective --seed 7

    cv.logloss_lowrank   = 0.478636
    cv.logloss_network   = 0.494646
    cv.diff_mean         = -0.016010   (< 0: low-rank wins, correct)
    cv.diff_se           = 0.001658
    structure.eig_dominance = 10.9312  (>> 3: one dimension, correct)

## Command 2: planted sparse-network structure
    python src/pipeline.py --demo network --seed 7

    cv.logloss_lowrank   = 0.663701
    cv.logloss_network   = 0.553808
    cv.diff_mean         = 0.109893   (> 0 by 29 SEs: network wins, correct)
    cv.diff_se           = 0.003754
    structure.eig_dominance = 1.2501  (< 3: no common cause, correct)
    structure.edge_density  = 0.1609  (true ~0.11-0.16 post-sampling)
    structure.edge_f1       = 0.8136

## What this establishes (PLAN.md T3)
The pipeline discriminates planted reflective from planted network
structure at n=800, p=30 in BOTH the CV comparison and the structure
diagnostics, with effects in opposite directions. It is therefore not
biased toward either theoretical outcome. Known limitations are
documented in src/models.py docstrings (held-out-predictor imputation in
cellwise_cv; person-fit power floor in tests/test_recovery.py).

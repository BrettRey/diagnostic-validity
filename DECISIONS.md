# DECISIONS — diagnostic-validity

2026-06-10 — Bib layout: references.bib set as symlink to central house-style bib; cgel2002, goodman1955, whiterawlins2016, whiterawlins2020 moved to references-local.bib. cgel2002 and goodman1955 duplicate central entries under project-internal keys to avoid changing main.tex keys.

2026-06-10 — Golden test blocker found during setup: src/models.py:65 uses l1_ratio=1.0 with liblinear solver (wrong parameter; should be penalty="l1"). Not fixed by executor per CLAUDE.md rule 3. Awaiting pm-update from Brett.

2026-06-10 — Golden blocker fixed (authorized by Brett via chat). models.py:65 l1_ratio=1.0 → penalty="l1". Verified in-memory before applying; pytest 4/4 after; both GOLDEN.md commands reproduce to 4 dp. GOLDEN.md left unchanged — anchors were already the correct l1 numbers, so the fix restores them (the prior "update GOLDEN.md" note was mistaken). sklearn 1.6.1.

2026-06-10 — Bib symlink REVERTED (pm directive, Brett via chat), superseding the symlink decision logged above. references.bib is now a project-local, verified-only regular file (not a symlink to central); references-local.bib removed and its \addbibresource line dropped from main.tex (single file). Reason: the verified-only invariant is a fabrication firewall — biber must fail loudly on any key not promoted from TO_VERIFY, which the central-bib symlink defeated; also keeps the repo self-contained. Duplicating central entries (croft2001, aarts2007, maling1983, matthews2014, cgel2002, goodman1955) is the accepted cost. whiterawlins2016/2020 stay OUT until pm promotion (Phase 2); the build fails on them by design. Overrides the portfolio-wide symlink+references-local workflow for this project.

2026-06-10 — Manifests record library versions (pm directive). pipeline.py writes python/numpy/scikit_learn/pandas into every manifest.json; GOLDEN.md notes anchors generated under sklearn 1.8.0 reproduce under 1.6.1 with penalty="l1". Documents the liblinear/l1_ratio cross-version bug class.

2026-06-10 — White & Rawlins 2016/2020 read (pm directive): lingbuzz 004596 PDF + publisher metadata. Bib details and the ordinal-model normalization summary logged to TO_VERIFY.md items 1, 2, D for pm promotion in Phase 2.

2026-06-10 — PHASE 1 PRE-REGISTRATION (pre-registered via pm-update, 2026-06-10; Brett). Verbatim:
1. Slice: past tense only (v1-equivalent) as primary; all-tense pooled mean as a sensitivity arm. Native-speaker filter as you proposed — endorsed, and report the (trivial) effect.
2. Normalization: participant-level z-scores (each rater standardized over their own responses), cell value = mean z across that cell's raters. Rationale: median five raters per cell makes rater scale-use the dominant nuisance, and per-rater standardization is the cheap, standard correction — we're not reproducing W&R's ordinal-model normalization, just citing it once read.
3. Primary threshold: z̄ > 0. The raw distribution being U-shaped means any mid-scale cut lands in the density trough, so the choice is less consequential than it looks — but z̄ > 0 specifically reads as "above this rater pool's own average," and its 0.356 base rate keeps frame columns furthest from degeneracy, which the BIC node fits need.
4. Sensitivity grid: z̄ > 0.25; raw mean ≥ 4; raw mean ≥ 4.5. Pre-registered robustness criterion: the verdict — sign of cv.diff_mean and the eigen-dominance band — must be invariant across the grid. If it isn't, threshold-dependence is itself the reported result; no arm gets promoted post hoc.
5. Continuous check: eigen spectrum of the undichotomized z̄ matrix reported alongside, satisfying PLAN §3's ordinal clause without new modeling code.
6. Missing cells: exclude verbs with any missing cell in the analyzed slice; report how many verbs that costs (bounded above by 264). Flag any frame with post-dichotomization base rate outside [0.02, 0.98]; they stay in, flagged.

Calibration note (pm, not a decision): with 1,007 verbs from a single putative category, the expected boring outcome is a strong first dimension (general embeddability); eigen-dominance alone will not adjudicate the theories on this dataset (it will on the Phase 3 cross-category grid). Informative quantities here: the CV comparison, residual structure after the first dimension, and which frames carry projectibility. High dominance is not a verdict for the reflective view.

2026-06-10 — GO/no-go: GO (pm ruling). T1 satisfied via its first (OR) clause — network wins held-out prediction in all five arms at ≥2 SE, sign-invariant; the non-dissociating structure diagnostics do not block the OR. T2 provisionally satisfied (48/49 frames >0.01 point; bootstrap since confirmed 49/49 with CI_lo>0) — pre-commitment stands: if <10 clear significance, halt and revisit. T3 holds. Single-category caveat for §4 drafting: the headline is "on this grid," not "in general."

2026-06-10 — AMENDMENT (pm, pre-registered via pm-update): dominance bands simplified to "dominant >3 / non-dominant <3" (the 1.5–3 intermediate band is dropped). Original PLAN §4 wording preserved: "first-eigenvalue dominance ratio > 3 vs < 1.5 across category subsets". The band-invariance "False" in the committed pilot (git 22e5027) was an artifact of values straddling the over-fine 1.5 cut; under the amended bands all pilot arms are "non-dominant" and the verdict is fully invariant. Applies to Phase 3 and any pilot re-run.

2026-06-10 — Phase 3 sampling frame, pre-registered via pm-update, 2026-06-10. Verbatim:

F1 — Strata and sizes (target ≈ 450 lexemes). (a) Adjectives, n = 220: sampled from lemmas POS-tagged adjective in the frequency frame (F2), stratified into four log-frequency bands by rank within tag — 1–500, 501–2,000, 2,001–10,000, 10,001–50,000 — 55 per band, simple random within band. (b) Prepositions: the full inventory of single-word prepositions as listed in CGEL ch. 7 — executor extracts the list from the chapter itself (source-grounded; expected order ~100; log the exact count and provenance). (c) Determinatives: the determinative list from Reynolds 2021's 138-item matrix, reused verbatim — already source-grounded and machine-readable. (d) Boundary set, n = 16: the forced-inclusion list in diagnostics.yaml, analyzed with and without, per RQ3. (e) Category-blind stratum, n = 60: random lemmas from the top 10,000 by frequency, drawn with no POS conditioning. This stratum is the structural answer to Croft's opportunism charge at the sampling stage: if cluster structure emerges among items selected with no category labels at all, the categories aren't artefacts of the seed lists.

F2 — Frequency frame. Primary: Kilgarriff's lemmatized BNC frequency list (free, POS-tagged, stable artifact); executor verifies availability, logs URL + file hash to the manifest before sampling. Fallback if unavailable or truncated: a stated open corpus tagged with a stated tagger version, logged the same way. Either way the frame file itself goes in data/ so the sample is reproducible from the repo alone.

F3 — Circularity firewall. Frame POS labels (and CGEL list membership) are used only to define strata for coverage and for known-groups validity checks. No analysis conditions on them as ground truth; the measurement models see an unlabeled lexeme × item matrix. This goes in PLAN §3 as an amendment and in the paper's methods verbatim-in-spirit.

F4 — Sampling mechanics. Seed = 26 for all draws. Sampling happens in one script, one commit, before any sentence is instantiated. Lexemes appearing in multiple strata (a frequent preposition also in the blind stratum) are deduplicated toward the more specific stratum, with the collision count logged.

F5 — Battery gate. Instantiation is blocked until TO_VERIFY item B clears: every source: field in diagnostics.yaml verified against the cited text. Item floor: ≥40 items per lexeme — current arithmetic is ~25 judgment items + ~18 corpus operationalizations ≈ 43, satisfying the power floor conditional on the battery surviving verification; if pruning drops it below 40, battery expansion comes back to the pm node before anything runs. Unconstructable lexeme × frame instantiations go in the template-exception table, never silent hand-edits.

F6 — Gold sample (DSL/PPI anchor). Simple random sample of 600 cells (uniform inclusion probability, seed = 26), drawn after the grid is instantiated and before any LLM rating exists — independence of gold from judge output is what makes the correction valid. Coding: me-the-human blind to judge ratings; a 200-cell double-coded subset for κ (Pullum is the candidate second coder — that's an email for you, not the executor). Published-judgment agreement (CGEL stars; Sprouse–Almeida hard region) remains judge validation, firewalled from the correction estimate, as pre-registered in the chat record.

F7 — Analysis mirror. Phase 3 runs the Phase 1 pipeline unchanged — same arms, same amended dominance bands (>3 / <3), bootstrap T2 procedure as now implemented — on the full grid and on per-stratum subsets, plus the RQ3 misfit ranking with the boundary set in/out. No new analytic degrees of freedom without a pm-update.

2026-06-10 — AMENDMENT F1(a)/F2 (pm, pre-registered via pm-update; supersedes the F1(a) within-tag-rank band spec and the F2 Kilgarriff-primary choice). The executor flagged that Kilgarriff's lemmatised BNC list has only 1,124 adjectives. Pm spec-error admission, verbatim:

"the original F1(a) band spec (rank-within-tag up to 10,001–50,000) was miscalibrated by me — no corpus list has 50,000 adjective lemmas, so the top band was unfillable under any frame, not just Kilgarriff's. The blocker is a spec error surfacing, which is what pre-registration is for. The amendment: band by Zipf value rather than within-tag rank — SUBTLEX-UK ships Zipf scores natively, they're the standard psycholinguistic frequency scale, and they make the bands corpus-size-independent. Four bands: Zipf ≥ 5; 4–5; 3–4; 2–3; 55 adjectives per band (dominant-PoS = adjective), simple random within band, seed 26. Report actual band populations; if the bottom band can't fill, merge 2–3 into 3–4 and log it."

Frame = SUBTLEX-UK (van Heuven, Mandera, Keuleers & Brysbaert 2014), pm option 1. Rationale: option 2 (full BNC list) rests on an unverified free-availability search; option 3 (keep Kilgarriff, re-scope) discards low-frequency adjectives where profiles fray (RQ3 power). SUBTLEX's subtitle register is a tolerable nuisance — frequency ranks correlate across corpora, the frame's job is coverage stratification, and the bleached sentences aren't register-bound. SUBTLEX dominant-PoS tags are noisy, which is acceptable under F3 (tags define strata, never ground truth; a mis-tagged noun in the adjective stratum is absorbed as category-blind noise) — note in methods.

F1c determinative stratum = 73 determinatives only (drop the 65 pronouns; off-target for the A/P/Det grid). F4 dedup applies where the 73 collide with the boundary set (more, less, enough, such expected to collide; log the count). Matrix source LingBuzz 005747; if it won't yield mechanically, pm drops a copy into data/ (don't burn a session scraping). Log file hash + provenance for the frame and the matrix.

2026-06-10 — Literature intake (5 sources read via Claude subagents to spare context; raw verbatim in intake/). Item B verification DRAFTED (paper/item_B_verification.md): 22/24 diagnostics sourced verbatim+pinpoint from CGEL (chs 5/6/7/19), Maling 1983, Matthews 2014; Aarts 2007 §8.4.4 corroborates (battery is CGEL's own H&P 2002:606). Surfaced for pm (all logged in TO_VERIFY): (a) 2 mis-attributed source fields — well_mod has NO valid source; gerund_gap → CGEL hollow clauses not Maling; (b) un_prefix/ness_nominalization are CGEL ch.19 not ch.6; (c) §1 mischaracterizes Aarts (he rejects graded MEMBERSHIP; use "subsective gradience / graded prototypicality"); (d) item C — Matthews 2014 does NOT support the §2.1 "diagnostics drifted/reweighted" claim (his drift = Sapir language-drift, p.173); soften §2.1; (e) worth/near disagreement (Maling worth=P, near=A vs CGEL worth=A, near=P; Aarts: configuration-dependent). Item 5 RESOLVED: Sprouse&Almeida 2012 = 98% min replication (JLing 48(3):609-652), SS&A 2013 = 95% convergence (Lingua 134:219-248) — different constructs. Pending pm: confirm item-B rows; resolve well_mod + coordination flags; §1/§2.1 rewrites; promote Sprouse cites. Web-methods cluster (Marsman/PPI/DSL/CoLA/vanderMaas) not yet fetched.

2026-06-10 — pm items applied (item A close + 2 flags resolved). (A) §2.1 *worth* sentence rewritten with pm-authored text (CGEL keeps *worth* in the adjective category, judging the case strong, and conducts the argument in its prepositions chapter — concedes liminality; \parencite[ch.7 §2.2]{cgel2002}); ⟨VERIFY⟩ dropped; build clean (xelatex+biber, no undefined cites). Item-A log given the clean UNBRACKETED CGEL quote (00-CGEL.md l.18595) per pm quotation caution (the only brackets in the passage are CGEL's own AdjP marking). (well_mod) CGEL §2.2 checked first per pm — NO *well* diagnostic there → well_mod = `pending-maling`, NON-gating (Maling not in drop pile → pm library scan; battery ≥40 without it). (coordination_with_adj) CGEL l.1277 shows coordination joins UNLIKE categories → weak/qualified test (keep, low discrimination). Item B now effectively COMPLETE (23/24 sourced + 1 pending-maling); attributive/postpositive already drafted from the Mendeley Matthews 2014, so NO Matthews session is needed for the item-B instantiation gate (Matthews time bears only on item C, resolved, + item-A calibration).

2026-06-10 — pm rewrites + rulings applied (intake follow-through; build verified clean). §1 Aarts clause rewritten (pm text: subsective gradience / graded prototypicality within sharp boundaries; utter/thin) — fixes the "graded membership" mischaracterization. §2.1 Matthews paragraph rewritten (pm text: criteria "quietly demoted"; verbatim \enquote{cluster of syntactic properties} (Matthews p.21) + \enquote{less than decisive} (p.24), confirmed char-for-char vs intake) — fixes the drift mischaracterization. §2.1 *worth* well-clause softened to judgment-only ("resists very / accepting well, inverting the pattern of central adjectives"; no source attributed). Sprouse cites PROMOTED to references.bib (sprousealmeida2012, sprouse2013; pm-authored entries); constructs-differ note (98% replication vs 95% convergence) deferred to §2.3 drafting. diagnostics.yaml: enough_postposed ADDED (battery -> 25; frame template QUEUED for pm confirmation; Maling 1983:263-265 / Aarts §8.4.4) per pm approval; coordination_with_adj note pre-registers low discrimination (CGEL l.1277, unlike-category coordination); well_mod = pending-maling (CGEL ch.7 modification also checked per pm hint -- no 'well'; novel-this-study fallback if Maling scan misses). Web-methods cluster dispatched (Marsman equivalence-scope, PPI, DSL, CoLA, van der Maas -> intake/, results pending). Mirror-image worth/near sentence drafted + QUEUED for pm confirmation (not inserted -- pm characterizations go through the same gate).

2026-06-10 — Web-methods intake RESULTS in (all 5 subagents; verbatim in intake/, cites queued for pm promotion in TO_VERIFY). Marsman 2018 (DOI 10.1080/00273171.2017.1379379): equivalence is STATISTICAL not theoretical/causal, low-rank a SEPARATE downstream approximation -> resolves §2.2 ⟨VERIFY: Marsman⟩ on those terms. PPI: Science 382(6671):669-674. DSL: NeurIPS/arXiv:2306.04746 (DSL-vs-PPI contrast not found, re-confirm). CoLA: 10,657 sentences, TACL 7:625-641 (item G flags an internal in-domain count discrepancy; cite the total). van der Maas 2006: PsychRev 113(4):842-861, "A single underlying g factor plays no role" -- existence proof, NOT anti-g. Items 3/6/7/8/12 resolved.

2026-06-10 — main.tex switched to the house-style preamble (\input{.house-style/preamble.tex}), replacing the barebones Phase-0 preamble. Stored as a project SNAPSHOT at paper/.house-style/preamble.tex (copy of central .house-style/preamble.tex), not a symlink -- self-contained for submission builds, avoiding the known relative-house-style-path blocker. XeLaTeX/fontspec (EB Garamond; Charis SIL/Inconsolata fallbacks), biblatex-APA with the preamble's conditional \addbibresource (loads the verified-only references.bib; references-local.bib absent so firewall intact), csquotes, langsci-gb4e, semantic macros (\term/\mention/...). Build verified clean (rc 0, no undefined cites, no missing glyphs). Note: snapshot can drift from central; refresh via copy if the house style updates.

2026-06-10 — pm item-B review + gate ruling (pm-update). pm read every item-B row against its verbatim quote; 21 confirmed outright. APPLIED to main.tex: (mirror) §2.1 worth/near mirror-image sentence INSERTED ("two most detailed treatments… the diagnostics underdetermine the verdict"); (§2.2) Marsman ⟨VERIFY⟩ replaced with the statistical-equivalence caution citing \parencite{marsman2018} (equivalence is of statistical models / data distribution; low-rank a downstream idealization; verify-on-planted-structure clause kept); (§2.2) Borsboom ⟨VERIFY⟩ resolved with \parencite{borsboomcramer2013}. → §1–2 now has ZERO ⟨VERIFY⟩ markers (only the §5 Study-2 PPI/DSL marker remains; gated on the estimator). PROMOTIONS: angelopoulos2023, egami2023, warstadt2019, vandermaas2006 added to references.bib (fields drafted from intake/ front matter, each with a provenance comment).
that_clause_complement STATUS FLIP: not sourced by its quote — CGEL [9](b) is about NON-expandable bare declaratives (clauses REJECTING "that") being preposition-licensed, the OPPOSITE of the "LEX that S" frame, and the adjective side (glad/aware that S) is unsupported. → needs-re-source, NON-gating, instantiates late; the bare-declarative configuration logged as a candidate future item (not entered by drift).
enough_preposed ADDED (pair of enough_postposed; battery → 26).
FIVE directives applied: (1) predicative_be note = low expected discrimination (CGEL l.16161, "be… value as a diagnostic is quite limited"); (2) seem_complement note updated to "largely exclude PP complements" (l.16161), stronger than the Phase-0 hedge; (3) right_mod + well_mod frames → optional "(something)" (parallel to very_mod; avoids confounding the modifier test with np_complement); (4) fused_head frame → single "Someone saw LEX."; (5) template_exceptions table created (inflectional_comparison LEX-er schema; enough-as-lexeme collision). gerund_gap re-sourced to CGEL hollow clauses (l.1935, worth); easy/tough-class second pinpoint is a TODO (non-gating).
CAVEATS ruled: DSL-vs-PPI → PPI primary (uniform-probability gold sample is its home case; Science citation), DSL mentioned as related framework with no contrast claim; final ruling when the estimator lands in src/ with a planted-truth test in tests/. CoLA → cite the total 10,657. van der Maas → §5 one move only ("a dominant first dimension does not entail a common cause"), never "g is fake".
GATE RULING — F5 CLEARS conditionally: instantiation authorized for the 24 confirmed judgment items + corpus columns the moment the two data/ files land; that_clause_complement and well_mod instantiate late as appended columns, not gates. Remaining blocker: the two pm file drops.

2026-06-11 — Phase 3 data acquired + F4 sampler run. SUBTLEX-UK frame fetched + verified (cleaned TSV, 160,024 types, LogFreq(Zipf) col 6 + DomPoS col 16 present -> no Zipf recompute; hashes + provenance in sampling/data_provenance.md; gitignored 18 MB, hash-reproducible). Reynolds matrix committed (data/reynolds2021_determinative_matrix.csv, 47 KB). Det/pron split SOURCED from Reynolds 2021 (intake/reynolds2021_det_pron_split.md): the paper gives counts + a dendrogram but no in-text list; the split is POSITIONAL (Appendix R: eqdist.etest sizes=c(73,65); filename 73-65full.csv) -- first 73 data rows = determinatives, verified (row 73 = various_det, row 74 = each_other). The 73 extracted verbatim -> sampling/determinatives_reynolds73.txt. F4 sampler (src/sample_phase3.py, seed 26): 522 unique lexemes -- boundary 16, determinative 67 (73 labels - 4 to boundary [enough/less/more/such] - 2 internal which/whichever dups), preposition 167 (168 - near->boundary), adjective 213 (220 drawn - 7 to more-specific strata), blind 59 (60 - strong->adjective). No band merge (Zipf bands 119/595/2131/3686, all >=55). 15 collisions logged in results/phase3_sample_manifest.json. NOTE: total 522 exceeds the F1 ~450 target, driven by the full 167-preposition inventory (the "tails in" ruling); pm decision if prepositions should be capped or split (full vs core96) for the grid. NEXT: instantiation (F5-gated; cleared for the 24 confirmed judgment items + corpus columns).
